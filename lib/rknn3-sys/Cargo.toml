[package]
name = "rknn3-sys"
version = "0.1.0"
edition = "2024"
description = "Safe Rust bindings for Rockchip RKNN3 Runtime API"
license = "MIT"

[dependencies]
thiserror = "2.0"
tracing = "0.1"

[dev-dependencies]
clap = { version = "4.0", features = ["derive"] }
