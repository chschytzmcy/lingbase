# Lingbase - 边缘 LLM 推理服务设计文档

> 版本：v3.0
> 更新日期：2026-05-14
> 状态：Phase 1 已完成，流式输出已实现

---

## 1. 项目概述

### 1.1 项目定位

Lingbase 是一个部署在边缘端的 LLM 模型推理服务，核心特性：

- **轻量高效**：基于 llama.cpp，无重型依赖
- **OpenAI 兼容**：支持 `/v1/chat/completions` API
- **流式输出**：支持 SSE 实时 token 流式返回
- **跨平台**：支持 x86/ARM CPU，可扩展 CUDA/NPU

### 1.2 当前实现状态

| 功能模块 | 状态 | 说明 |
|----------|------|------|
| llama.cpp FFI | ✅ 已完成 | Model/Context/Batch/Tokenizer 封装 |
| CPU Backend | ✅ 已完成 | 支持同步推理和流式推理 |
| HTTP API | ✅ 已完成 | OpenAI 兼容接口 |
| 流式输出 (SSE) | ✅ 已完成 | 实时 token 流式返回 |
| 配置管理 | ✅ 已完成 | TOML 配置文件 |
| 健康检查 | ✅ 已完成 | `/health` 端点 |
| 结构化日志 | ✅ 已完成 | tracing + JSON 格式 |
| CUDA Backend | 📋 待实现 | GPU 加速推理 |
| Slot Pool | 📋 待实现 | 并发请求调度 |
| Continuous Batching | 📋 待实现 | 批处理优化 |
| 认证/速率限制 | 📋 待实现 | API Key + Token Bucket |
| Prometheus Metrics | 📋 待实现 | 性能监控指标 |

---

## 2. 系统架构

### 2.1 当前架构

```
┌─────────────────────────────────────────────────────────────────┐
│                      Lingbase Service                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                   HTTP API Layer (Axum)                  │   │
│  │              /v1/chat/completions                        │   │
│  │              /health                                     │   │
│  │          ┌─────────────┬─────────────┐                   │   │
│  │          │ 非流式响应   │  SSE 流式   │                   │   │
│  │          │ (JSON)      │  (Stream)   │                   │   │
│  │          └─────────────┴─────────────┘                   │   │
│  └─────────────────────────────────────────────────────────┘   │
│                           │                                    │
│                           ▼                                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              Backend Abstraction Layer                   │   │
│  │   ┌─────────────────────────────────────────────────┐   │   │
│  │   │       InferenceBackend Trait                     │   │   │
│  │   │  - forward()      → 同步推理                     │   │   │
│  │   │  - forward_stream() → 流式推理                   │   │   │
│  │   │  - tokenize/detokenize                           │   │   │
│  │   └─────────────────────────────────────────────────┘   │   │
│  │                          │                              │   │
│  │           ┌──────────────┼──────────────┐              │   │
│  │           │              │              │              │   │
│  │       ┌───▼───┐      ┌───▼───┐      ┌───▼───┐          │   │
│  │       │  CPU  │      │ CUDA  │      │ RKLLM │          │   │
│  │       │(实现) │      │(待定) │      │(待定) │          │   │
│  │       └───┬───┘      └───┬───┘      └───┬───┘          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                           │                                    │
│                           ▼                                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                llama.cpp FFI Layer                       │   │
│  │   ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐   │   │
│  │   │  Model   │ │ Context  │ │  Batch   │ │Tokenizer │   │   │
│  │   │ Wrapper  │ │ Wrapper  │ │ Wrapper  │ │ Wrapper  │   │   │
│  │   └──────────┘ └──────────┘ └──────────┘ └──────────┘   │   │
│  └─────────────────────────────────────────────────────────┘   │
│                           │                                    │
│                           ▼                                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                 Infrastructure Layer                     │   │
│  │   ┌──────────┐ ┌──────────┐ ┌──────────┐               │   │
│  │   │  Config  │ │  Health  │ │ Logging  │               │   │
│  │   │  (TOML)  │ │  Check   │ │(tracing) │               │   │
│  │   └──────────┘ └──────────┘ └──────────┘               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 组件职责

| 组件 | 文件 | 职责 |
|------|------|------|
| HTTP API | `src/api/http.rs` | OpenAI 兼容接口，流式/非流式响应 |
| API Types | `src/api/types.rs` | 请求/响应数据结构定义 |
| Backend Trait | `src/backend/backend.rs` | 推理后端抽象接口 |
| CPU Backend | `src/backend/cpu.rs` | CPU 推理实现，包含流式推理 |
| Backend Factory | `src/backend/factory.rs` | 后端选择与创建 |
| LlamaModel | `src/llama/model.rs` | 模型加载与生命周期管理 |
| LlamaContext | `src/llama/context.rs` | 推理上下文管理 |
| Batch | `src/llama/batch.rs` | Token batch 构建 |
| Tokenizer | `src/llama/tokenize.rs` | 文本 tokenize/detokenize |
| FFI | `src/llama/ffi.rs` | llama.cpp C API 绑定 |
| Config | `src/infra/config.rs` | 配置文件加载 |
| Health | `src/infra/health.rs` | 健康检查实现 |
| Logging | `src/infra/logging.rs` | 结构化日志初始化 |
| Error | `src/error.rs` | 统一错误类型 |

---

## 3. 核心接口设计

### 3.1 HTTP API

#### 请求格式

```
POST /v1/chat/completions
Content-Type: application/json

{
  "model": "Qwen3-4B",
  "messages": [
    {"role": "user", "content": "你好"}
  ],
  "max_tokens": 256,
  "temperature": 0.7,
  "top_p": 0.9,
  "stream": false    // true 启用流式输出
}
```

#### 响应格式 (非流式)

```json
{
  "id": "chatcmpl-xxx",
  "object": "chat.completion",
  "created": 1234567890,
  "model": "Qwen3-4B",
  "choices": [{
    "index": 0,
    "message": {"role": "assistant", "content": "..."},
    "finish_reason": "stop"
  }],
  "usage": {
    "prompt_tokens": 10,
    "completion_tokens": 50,
    "total_tokens": 60
  }
}
```

#### 响应格式 (流式)

```
data: {"id":"chatcmpl-xxx","object":"chat.completion.chunk",...}
data: {"id":"chatcmpl-xxx","object":"chat.completion.chunk","choices":[{"delta":{"content":"你"}}]}
data: {"id":"chatcmpl-xxx","object":"chat.completion.chunk","choices":[{"delta":{"content":"好"}}]}
...
data: {"id":"chatcmpl-xxx","object":"chat.completion.chunk","choices":[{"finish_reason":"stop"}]}
```

### 3.2 Backend Trait

```rust
pub trait InferenceBackend: Send + Sync {
    // 基本信息
    fn name(&self) -> &str;
    fn health_check(&self) -> bool;
    fn memory_stats(&self) -> MemoryStats;
    fn max_context_size(&self) -> usize;
    
    // 同步推理
    fn forward(&self, tokens: &[i32], config: &InferenceConfig) 
        -> InferenceResult<ForwardResult>;
    
    // 流式推理 (新增)
    fn forward_stream(&self, tokens: &[i32], config: &InferenceConfig) 
        -> Pin<Box<dyn Stream<Item = InferenceResult<StreamToken>> + Send>>;
    
    // 文本处理
    fn tokenize(&self, text: &str) -> InferenceResult<Vec<i32>>;
    fn detokenize(&self, tokens: &[i32]) -> InferenceResult<String>;
    
    // Sampling
    fn sample_token(&self, logits: &[f32], config: &InferenceConfig) -> i32;
}
```

### 3.3 流式推理实现

流式推理使用 `tokio::sync::mpsc` channel 实现：

```rust
fn forward_stream(&self, tokens: &[i32], config: &InferenceConfig) 
    -> Pin<Box<dyn Stream<Item = InferenceResult<StreamToken>> + Send>> 
{
    let (tx, rx) = tokio::sync::mpsc::channel(16);
    
    tokio::task::spawn_blocking(move || {
        // Prefill
        ctx.decode(batch)?;
        
        // Generation loop - 每个 token 立即发送
        for each_token {
            let text = tokenizer.decode(&[token]);
            tx.blocking_send(Ok(StreamToken {
                token, text, is_first, is_done
            }));
        }
    });
    
    Box::pin(ReceiverStream::new(rx))
}
```

关键点：
- 使用 `spawn_blocking` 避免阻塞 Tokio runtime
- Channel buffer 16 提供 backpressure
- 每个 token 通过 `blocking_send` 立即推送

---

## 4. 数据结构

### 4.1 推理配置

```rust
pub struct InferenceConfig {
    pub max_tokens: usize,      // 最大生成 tokens
    pub temperature: f32,       // 温度参数
    pub top_p: f32,             // Top-p sampling
    pub top_k: i32,             // Top-k sampling
    pub repeat_penalty: f32,    // 重复惩罚
    pub timeout_ms: Option<u64>, // 超时时间
}
```

### 4.2 推理结果

```rust
pub struct ForwardResult {
    pub tokens: Vec<i32>,           // 生成的 token IDs
    pub first_token_ms: Option<u64>, // 首 token 延迟
    pub total_ms: u64,              // 总耗时
}

pub struct StreamToken {
    pub token: i32,       // Token ID
    pub text: String,     // 解码文本
    pub is_first: bool,   // 是否首 token
    pub is_done: bool,    // 是否完成
}
```

### 4.3 错误类型

```rust
pub enum InferenceError {
    ModelLoadFailed(String),
    InvalidPath(String),
    TokenizationFailed(String),
    PromptTooLong(usize, usize),
    Timeout(Duration),
    InferenceFailed(String),
    BackendNotInitialized,
    OutOfMemory(String),
    ...
}
```

---

## 5. 配置管理

### 5.1 配置文件结构

```toml
# config/environment.toml

[server]
host = "0.0.0.0"
port = 11017
workers = 4

[backend]
supported = ["cuda", "cpu"]
auto_detect = true

[model]
test_model_path = "/path/to/model.gguf"
context_size = 8192
max_prompt_tokens = 8192
max_generation_tokens = 2048
```

### 5.2 配置加载

```rust
pub struct AppConfig {
    pub server: ServerConfig,
    pub backend: BackendConfig,
    pub model: ModelConfig,
}

impl AppConfig {
    pub fn load() -> Result<Self, ConfigError> {
        Config::builder()
            .add_source(File::with_name("config/environment"))
            .build()?
            .try_deserialize()
    }
}
```

---

## 6. Qwen3 Chat Template

当前使用 Qwen3 的 chat template：

```rust
// 构建 prompt
let prompt = messages.iter()
    .map(|m| format!("<|im_start|>{}\n{}<|im_end|>", m.role, m.content))
    .join("\n");

// 添加 assistant start token + 禁用思考模式
prompt.push_str("<|im_start|>assistant\n/no_think\n");
```

说明：
- `<|im_start|>` / `<|im_end|>`: Qwen3 消息边界标记
- `/no_think`: 禁用 Qwen3 的思考模式，直接输出回答

---

## 7. 项目结构

```
lingbase/
├── Cargo.toml                 # Rust 项目配置
├── build.rs                   # llama.cpp CMake 构建脚本
├── run.sh                     # 启动脚本 (架构自动检测)
│
├── src/
│   ├── main.rs                # 入口
│   ├── lib.rs                 # 库入口
│   ├── error.rs               # 统一错误类型
│   │
│   ├── api/                   # API 层
│   │   ├── mod.rs
│   │   ├── http.rs            # Axum HTTP handlers (支持流式)
│   │   └── types.rs           # OpenAI 兼容类型定义
│   │
│   ├── backend/               # 后端抽象层
│   │   ├── mod.rs
│   │   ├── backend.rs         # InferenceBackend trait
│   │   ├── cpu.rs             # CPU 后端实现 (含流式)
│   │   └── factory.rs         # Backend Factory
│   │
│   ├── llama/                 # llama.cpp FFI 封装
│   │   ├── mod.rs
│   │   ├── model.rs           # LlamaModel wrapper
│   │   ├── context.rs         # LlamaContext wrapper
│   │   ├── batch.rs           # Batch 管理
│   │   ├── tokenize.rs        # Tokenizer
│   │   ├── ffi.rs             # C API 绑定
│   │   └── ptr.rs             # 指针类型定义
│   │
│   └── infra/                 # 基础设施
│       ├── mod.rs
│       ├── config.rs          # TOML 配置管理
│       ├── health.rs          # 健康检查
│       └── logging.rs         # tracing 日志
│
├── config/
│   └── environment.toml       # 默认配置
│
├── lib/                       # 预编译 llama.cpp 库
│   ├── x86_64/
│   │   └── libllama.so
│   └── aarch64/
│       └── libllama.so
│
└── docs/
    ├── 架构设计文档_v2.0.md   # (历史版本)
    └── gguf-quantization.md   # GGUF 量化说明
```

---

## 8. 依赖关系

```toml
[dependencies]
tokio = { version = "1.43", features = ["full"] }
axum = { version = "0.8", features = ["macros"] }
tower = "0.5"
tower-http = { version = "0.6", features = ["trace", "cors"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
config = "0.15"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }
futures = "0.3"
tokio-stream = "0.1"
thiserror = "2.0"
anyhow = "1.0"
uuid = { version = "1.11", features = ["v4", "serde"] }
```

---

## 9. 后续开发计划

### Phase 2: CUDA Backend (预计 2 周)

- CUDA 后端实现
- GPU 显存管理
- 后端自动检测优化

### Phase 3: 并发调度 (预计 2 周)

- Slot Pool 实现
- 请求队列管理
- 三级超时控制

### Phase 4: 性能优化 (预计 2 周)

- Continuous Batching
- Prometheus Metrics
- 速率限制

### Phase 5: 边缘特性 (预计 1 周)

- 看门狗监控
- 自愈机制
- 降级策略

---

## 10. 测试验证

### 10.1 功能测试

```bash
# 非流式请求
curl -X POST http://localhost:8080/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{"model":"Qwen3-4B","messages":[{"role":"user","content":"你好"}],"max_tokens":64}'

# 流式请求
curl -X POST http://localhost:8080/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{"model":"Qwen3-4B","messages":[{"role":"user","content":"你好"}],"stream":true,"max_tokens":64}'

# 健康检查
curl http://localhost:8080/health
```

### 10.2 性能指标

| 指标 | 目标值 | 测试方法 |
|------|--------|----------|
| 首 Token 延迟 | < 500ms | 流式请求首 chunk 时间 |
| Token/s | > 20 | 流式输出速度 |
| 内存占用 | < 模型大小 + 500MB | 运行时监控 |

---

## 11. 参考资料

- [llama.cpp 官方仓库](https://github.com/ggerganov/llama.cpp)
- [OpenAI API 规范](https://platform.openai.com/docs/api-reference)
- [Axum Web Framework](https://github.com/tokio-rs/axum)
- [GGUF 量化说明](./gguf-quantization.md)

---

*文档版本：v3.0*
*最后更新：2026-05-14*
*实现状态：Phase 1 完成，流式输出已实现*